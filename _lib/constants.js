const ENTRY            = "app"
// const ENTRY_FILENAME   = "[name].[contenthash].js"

const OUTPUT_FILENAME  = "[name].js"
const OUTPUT_DIR_NAME  = "dist"

module.exports = {
	ENTRY,
	OUTPUT_DIR_NAME,
	OUTPUT_FILENAME,
}